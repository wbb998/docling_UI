# Docling 前端界面项目需求文档

## 1. 项目概述

### 1.1 项目背景
Docling 是一个功能强大的后端文档处理工具，能够将多种格式的文档（PDF、图片、音频等）转换为结构化的数据格式（Markdown、JSON、HTML等）。当前，该工具主要通过命令行界面（CLI）进行操作，这对于非技术用户不够友好，也限制了其在更广泛场景下的应用。

### 1.2 项目目标
为了提升用户体验和易用性，本项目旨在为 Docling 后端系统开发一个现代、直观、用户友好的图形化前端界面（GUI）。该界面将封装后端的复杂功能，以可视化的方式引导用户完成文档转换任务，并提供丰富的交互和结果展示功能。

## 2. 功能需求

### 新增：FR0: 界面模式切换
- **FR0.1 模式切换**: 界面应提供一个明显的开关或选项，允许用户在 **“简单模式”** 和 **“高级模式”** 之间切换。
  - **简单模式**: 默认模式，界面极简，只保留核心的文件上传和转换功能，隐藏所有高级配置。旨在让用户以最快速度完成标准转换任务。
  - **高级模式**: 为专业用户设计，暴露所有可配置的选项，如流水线选择、OCR配置、内容增强等。

### 2.1 文件上传与管理 (适用于两种模式)
- **FR1.1 单文件上传**: 用户可以从本地计算机选择并上传单个文件。
- **FR1.2 多文件上传**: 用户可以一次性选择并上传多个文件。
- **FR1.3 文件夹上传**: 用户可以选择并上传整个文件夹，前端应能递归读取文件夹内的所有文件。
- **FR1.4 URL输入**: 用户可以输入一个或多个网络文件的URL进行添加。
- **FR1.5 文件队列**: 上传的文件应显示在一个清晰的队列中，包含文件名、大小、格式和状态（待处理、处理中、成功、失败）。
- **FR1.6 文件操作**: 用户可以从队列中移除单个或所有文件。

### 2.2 转换配置 (高级模式)

高级模式下的配置应遵循模块化设计，允许用户自由组合各项功能。前端界面应根据用户的选择动态显示或隐藏相关选项。

#### **FR2.1 核心目标 (Output Mode)**
这是最高级别的配置，决定了任务的主要目标。应作为一组互斥的选项（如选项卡或单选按钮）提供。

- **文档转换 (Conversion)**: 将输入文档转换为一种或多种指定格式。
- **文档分块 (Chunking)**: 将文档内容分割成结构化的数据块，通常用于RAG。
- **信息提取 (Extraction)**: 根据用户定义的Schema从文档中提取特定信息。

---

#### **FR2.2 全局与通用配置**
这些配置项适用于多种核心目标。

- **FR2.2.1 输入格式 (`from_formats`)**:
  - 提供多选框，允许用户限定只处理特定格式的输入文件（如 `PDF`, `IMAGE`）。留空则处理所有支持的格式。
- **FR2.2.2 输出目录 (`output`)**:
  - 提供一个输入框和文件夹选择按钮，允许用户指定一个目录来保存所有转换结果。留空则结果保存在源文件各自的目录中。
- **FR2.2.3 图像处理 (`image_export_mode`)**:
  - 提供单选按钮让用户选择图像的导出方式：`嵌入式 (embedded)`, `引用式 (referenced)`, `占位符 (placeholder)`。
- **FR2.2.4 硬件与性能**:
  - **PDF后端 (`pdf_backend`)**: 提供下拉菜单选择PDF处理后端 (`DoclingParseV4`, `PyPdfium2`)。
  - **硬件加速 (`device`)**: 提供下拉菜单选择设备 (`auto`, `cpu`, `cuda`, `mps`)，并允许为 `cuda` 指定设备ID（如 `cuda:1`）。
  - **线程数 (`num_threads`)**: 提供数字输入框。
- **FR2.2.5 错误处理**:
  - **错误时中止 (`abort_on_error`)**: 提供复选框。
  - **文档超时 (`document_timeout`)**: 提供数字输入框（单位：秒）。
- **FR2.2.6 远程服务**:
  - **允许远程服务 (`enable_remote_services`)**: 提供一个总开关复选框。**此选项是所有远程功能（如远程VLM、远程图片描述API）的前置条件。**

---

#### **FR2.3 处理流水线配置 (Pipeline)**
用户选择不同的流水线，将决定文档内容如何被理解和解析。

- **FR2.3.1 流水线选择 (`pipeline`)**:
  - 提供下拉菜单或单选按钮选择：`Standard`, `VLM`, `ASR`。
- **FR2.3.2 Standard 流水线专属配置**:
  - **OCR配置**:
    - [ ] **启用OCR (`ocr`)**: 复选框。
    - 当启用OCR时，显示以下选项：
        - **OCR引擎 (`ocr_engine`)**: 下拉菜单选择 (`easyocr`, `tesseract`, `rapidocr` 等)。
        - **OCR语言 (`ocr_lang`)**: 文本框或多选框。
        - [ ] **强制OCR (`force_ocr`)**: 复选框。
        - [ ] **Tesseract语言自动检测 (`ocr_lang_auto`)**: 复选框 (仅当引擎为Tesseract时显示)。
        - **RapidOCR自定义模型**: 文本输入框 (仅当引擎为RapidOCR时显示)。
  - **内容增强配置**:
    - [ ] **代码增强 (`enrich_code`)**: 复选框。
    - [ ] **公式增强 (`enrich_formula`)**: 复选框。
    - [ ] **图片分类 (`enrich_picture_classes`)**: 复选框。
- **FR2.3.3 VLM 流水线专属配置 (`vlm_model`, `vlm_options`)**:
  - **模式选择**: 提供“本地模型”和“远程API”两种模式选择。
  - **本地模型**: 提供下拉菜单选择推理框架 (`Transformers`, `MLX`) 和具体的模型 (`smoldocling`, `granite-vision` 等)。
  - **远程API**: 提供预设服务 (`LM Studio`, `Ollama`, `watsonx.ai`) 和“自定义”选项，选择后显示表单填写API URL、参数、Headers等。
- **FR2.3.4 ASR 流水线专属配置 (`asr_model`)**:
  - **ASR模型选择**: 提供下拉菜单选择具体的ASR模型 (`whisper_tiny`, `whisper_base`, `whisper_turbo` 等)。

---

#### **FR2.4 特定目标配置**
这些配置仅在选择了对应的“核心目标 (FR2.1)”时显示。

- **FR2.4.1 文档转换 (Conversion) 配置**:
  - **输出格式 (`to_formats`, `to_doctags`, etc.)**:
    - 提供多选框选择多种标准格式 (`Markdown`, `HTML`, `JSON`, `YAML`, `Text`)。
    - 提供单独的文本输入框用于指定特定格式的文件名输出，如 `to_doctags`, `to_text`, `to_html_split_pages`。这些选项与多选框应为互斥关系。
- **FR2.4.2 文档分块 (Chunking) 配置**:
  - **输出格式**: 自动锁定为 `JSON`。
  - **分块策略 (`chunking_strategy`)**: 提供下拉菜单选择 (如 `by_element`)。
  - **排除元素 (`exclude`)**: 提供多选框或标签输入框排除元素 (如 `picture`, `table`)。
- **FR2.4.3 信息提取 (Extraction) 配置**:
  - **输出格式**: 自动锁定为 `JSON`。
  - **JSON Schema (`extraction_schema`)**: 提供一个文本域，允许用户粘贴或编写JSON Schema。建议提供实时语法验证。

---

#### **FR2.5 附加功能与产物**
这些功能可以与主要目标组合使用，作为附加处理步骤或产物。

- **FR2.5.1 图片描述生成 (Picture Annotation)**:
  - [ ] **启用图片描述 (`do_picture_description`)**: 一个总开关复选框。
  - 当启用时，显示以下配置：
    - **模式**: “本地模型”或“远程API”。
    - **本地模型配置 (`picture_description_options`)**:
        - **模型选择**: 下拉菜单，包含预设模型和“自定义”。
        - **自定义Repo ID**: 文本输入框。
        - **提示词 (Prompt)**: 文本区域。
    - **远程API配置 (`picture_description_options`)**:
        - **前置条件**: 必须已勾选 `enable_remote_services`。
        - **API URL**: 文本输入框。
        - **Headers/Params**: JSON格式的文本区域。
        - **提示词 (Prompt)**: 文本区域。
- **FR2.5.2 额外产物导出 (Artifact Export)**:
  - 提供一组复选框，允许用户在转换的同时导出额外的产物：
    - [ ] **导出表格 (CSV)**
    - [ ] **导出表格 (HTML)**
    - [ ] **导出页面为图像 (PNG)**
    - [ ] **导出文档内图表为图像 (PNG)**
    - [ ] **导出多模态数据 (Parquet)**
- **FR2.5.3 文档后处理**:
  - [ ] **文本翻译**: 提供复选框和目标语言输入框。

### 2.3 任务执行与监控
- **FR3.1 开始转换**: 一个“开始转换”按钮，点击后触发后端处理。
- **FR3.2 进度显示**:
  - **总体进度**: 显示整个转换任务的总体进度条。
  - **单个文件进度**: 在文件队列中实时更新每个文件的处理状态。
- **FR3.3 实时日志**: 提供一个可展开的日志窗口，实时显示后端的处理日志，帮助用户了解当前状况和排查问题。
- **FR3.4 任务控制**: 提供“取消”按钮，可以中止正在进行中的转换任务。

### 2.4 结果展示与下载
- **FR4.1 结果概览**: 转换完成后，提供一个清晰的结果摘要页面，显示成功和失败的文件数量。
- **FR4.2 单文件结果预览**:
  - 用户可以点击成功转换的文件，在界面上直接预览其内容。
  - **Markdown/Text**: 在代码编辑器或格式化文本区域中显示。
  - **HTML**: 在一个内嵌的iframe或新标签页中渲染显示。
  - **JSON**: 在一个可折叠、格式化的JSON查看器中显示。
- **FR4.3 结果下载**:
  - **单个下载**: 用户可以下载单个转换结果文件。
  - **产物下载**: 如果用户选择了导出额外产物（如表格、图像），这些产物应与主结果文件一起列出并提供单独下载。
  - **打包下载**: 提供一个“全部下载”按钮，将所有结果文件和导出的产物打包成一个ZIP文件供用户下载。
- **FR4.4 错误报告**: 对于转换失败的文件，应明确展示错误信息，并链接到相关的日志条目。

## 3. 非功能需求

### 3.1 用户体验 (UX)
- **NF1.1 易用性**: 界面设计应直观简洁，即使是初次使用的用户也能在没有指导的情况下完成基本操作。
- **NF1.2 响应式设计**: 界面应能适应不同尺寸的屏幕，在桌面浏览器上提供良好体验。
- **NF1.3 交互反馈**: 所有用户操作都应有即时、明确的视觉反馈（如加载动画、状态变化、成功/错误提示）。

### 3.2 性能
- **NF2.1 前端性能**: 界面应保持流畅，即使在处理大量文件队列时也不会出现卡顿。
- **NF2.2 大文件处理**: 前端应能处理大文件的上传，并提供上传进度。

### 3.3 兼容性
- **NF3.1 浏览器兼容**: 支持主流现代浏览器，如 Chrome, Firefox, Safari, Edge的最新版本。

### 3.4 可靠性
- **NF4.1 状态同步**: 前端必须准确地与后端同步任务状态，避免信息不一致。
- **NF4.2 错误处理**: 优雅地处理后端API的错误，并向用户提供清晰的错误提示。

## 4. 技术选型建议

### 4.1 前端框架
- **推荐**: **React** 或 **Vue.js**
  - **理由**: 两者都拥有庞大的社区、丰富的生态系统和成熟的组件库，能够快速构建功能丰富、交互性强的单页应用（SPA）。React的 `Next.js` 或 Vue 的 `Nuxt.js` 框架可以进一步简化开发。

### 4.2 UI 组件库
- **推荐**: **Ant Design** (React/Vue), **Element Plus** (Vue), or **Material-UI (MUI)** (React)
  - **理由**: 提供了一套高质量、开箱即用的UI组件，可以大大加快开发速度，并保证界面风格的统一性和专业性。

### 4.3 状态管理
- **推荐**: **Redux Toolkit** (React) 或 **Pinia** (Vue)
  - **理由**: 用于集中管理应用的全局状态（如文件队列、任务状态、配置选项），使状态逻辑清晰、可预测。

### 4.4 后端通信
- **推荐**: **Axios**
  - **理由**: 一个成熟的、基于Promise的HTTP客户端，支持请求/响应拦截、取消请求等高级功能，非常适合与后端RESTful API进行通信。

### 4.5 打包与构建
- **推荐**: **Vite**
  - **理由**: 提供极快的冷启动速度和热模块更新（HMR），能显著提升开发体验。

### 4.6 桌面应用打包 (可选)
- **推荐**: **Tauri** 或 **Electron**
  - **理由**: 如果需要将Web应用打包成跨平台的桌面应用，Tauri（基于Rust）提供了更轻量、更安全的方案，而Electron（基于Node.js）则拥有更成熟的生态。

---

## 2.6 配置补充与动态校验（新增）

- 新增全局与性能控件：
  - `page_batch_size`：数字输入，建议由后端返回推荐默认值。
  - `verbosity`/`log_level`：下拉框（`error/warn/info/debug`），默认 `info`。
  - 插件开关：`allow_external_plugins`、`show_external_plugins`（仅高级模式可见）。
- 图片描述生成扩展控件：
  - `images_scale`：数字输入（默认 2.0）。
  - `generate_picture_images`：复选框（默认启用）。
- 动态校验规则：
  - 选择 `Extraction` 或 `Chunking` 时：禁用 `to_formats`，显示“输出固定为 JSON”的提示。
  - 远程图片描述/远程 VLM：必须先启用 `enable_remote_services`，否则表单校验报错并禁止提交。
  - `image_export_mode` 仅影响 HTML/Markdown 等富文本；仅 JSON 输出且未导出 `pages_png/pictures_png` 时，该选项对结果无影响（显示提示）。
- 安全说明：
  - 远程服务凭据仅由后端从环境变量读取；前端不持久化明文密钥，日志展示需脱敏。

## 2.7 任务与可观测性（新增）

- 异步任务：
  - `/convert` 返回 `job_id`；前端可轮询 `/jobs/{id}/status` 或使用 SSE/WebSocket 订阅实时进度。
  - 前端在请求头附带 `X-Request-Id` 与 `Idempotency-Key`；后端支持幂等重试，避免重复执行。
- 进度显示：
  - 总体进度 + 单文件进度；失败文件提供中文错误与日志链接。

## 2.8 配置预设（新增）

- 支持导入/导出配置 JSON。
- 允许“保存为预设/一键应用预设”，便于复用常见配置组合。