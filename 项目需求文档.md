# Docling 前端界面项目需求文档

## 1. 项目概述

### 1.1 项目背景
Docling 是一个功能强大的后端文档处理工具，能够将多种格式的文档（PDF、图片、音频等）转换为结构化的数据格式（Markdown、JSON、HTML等）。当前，该工具主要通过命令行界面（CLI）进行操作，这对于非技术用户不够友好，也限制了其在更广泛场景下的应用。

### 1.2 项目目标
为了提升用户体验和易用性，本项目旨在为 Docling 后端系统开发一个现代、直观、用户友好的图形化前端界面（GUI）。该界面将封装后端的复杂功能，以可视化的方式引导用户完成文档转换任务，并提供丰富的交互和结果展示功能。

## 2. 功能需求

### 新增：FR0: 界面模式切换
- **FR0.1 模式切换**: 界面应提供一个明显的开关或选项，允许用户在 **“简单模式”** 和 **“高级模式”** 之间切换。
  - **简单模式**: 默认模式，界面极简，只保留核心的文件上传和转换功能，隐藏所有高级配置。旨在让用户以最快速度完成标准转换任务。
  - **高级模式**: 为专业用户设计，暴露所有可配置的选项，如流水线选择、OCR配置、内容增强等。

### 2.1 文件上传与管理 (适用于两种模式)
- **FR1.1 单文件上传**: 用户可以从本地计算机选择并上传单个文件。
- **FR1.2 多文件上传**: 用户可以一次性选择并上传多个文件。
- **FR1.3 文件夹上传**: 用户可以选择并上传整个文件夹，前端应能递归读取文件夹内的所有文件。
- **FR1.4 URL输入**: 用户可以输入一个或多个网络文件的URL进行添加。
- **FR1.5 文件队列**: 上传的文件应显示在一个清晰的队列中，包含文件名、大小、格式和状态（待处理、处理中、成功、失败）。
- **FR1.6 文件操作**: 用户可以从队列中移除单个或所有文件。

### 2.2 转换配置

#### 2.2.1 简单模式下的配置
- **无可见配置**: 在简单模式下，所有配置项都使用系统默认值（例如：转换为Markdown，使用标准流水线），用户界面上不显示任何配置选项。

#### 2.2.2 高级模式下的配置
- **FR2.0 输出模式选择 (Output Mode)**:
  - 提供一组选项（如单选按钮或选项卡）让用户选择输出的模式：
    - **文档转换 (Document Conversion)**: 默认模式，将整个文档转换为所选格式。
    - **分块输出 (Chunking)**: 将文档转换为结构化块，用于RAG等下游应用。
    - **信息提取 (Information Extraction)**: 从文档中提取特定的、结构化的信息。
- **FR2.1 流水线选择**: 提供一个简单明了的方式让用户选择处理流水线：
  - **标准流水线 (Standard)**: 用于处理PDF和图片，提供OCR和内容增强选项。
  - **视觉语言模型 (VLM)**: 用于需要高级视觉理解的文档。
  - **语音识别 (ASR)**: 用于处理音频和视频文件。
- **FR2.2 输出格式选择 (当模式为“文档转换”时)**:
  - 用户可以选择一种或多种输出格式，如 `Markdown`, `HTML`, `JSON`, `YAML`, `Text`, `DOCTAGS`。
  - 应提供一个“全选”选项。
- **FR2.3 OCR配置 (当选择Standard流水线时)**:
  - **启用/禁用OCR**: 一个开关控件。
  - **OCR引擎**: 提供一个下拉菜单，让用户选择OCR引擎，如 `easyocr`, `tesseract`, `rapidocr` 等。
  - **OCR语言**: 一个下拉菜单或多选框，允许用户选择识别语言。
  - **强制OCR**: 一个复选框，用于强制对整个页面进行OCR。
  - **语言自动检测**: (当选择Tesseract时) 提供一个复选框以启用语言自动检测。
  - **自定义RapidOCR模型**: (当选择RapidOCR时) 提供文本输入框让用户指定自定义ONNX模型的路径。
- **FR2.4 内容增强配置 (当选择Standard流水线时)**:
  - **代码/公式/图片分类**: 提供复选框让用户启用/禁用代码、公式和图片分类的增强功能。
  - **图片描述生成 (Picture Annotation)**:
    - 提供一个总开关复选框 **“启用图片描述生成”** (`do_picture_description`)。
    - 当启用时，显示以下配置选项：
        - **模式选择**: 提供“本地模型”和“远程API”两种模式的单选按钮。
        - **本地模型配置** (当选择“本地模型”时):
            - **模型选择**: 提供一个下拉菜单，包含预设的常用模型（如 `ibm-granite/granite-vision`, `HuggingFaceTB/SmolVLM`）以及一个“自定义”选项。
            - **自定义模型Repo ID**: 当选择“自定义”时，显示一个文本输入框，让用户填写 Hugging Face Hub 上的模型 `repo_id`。
            - **提示词 (Prompt)**: 提供一个文本区域，让用户输入自定义的提示词来指导模型生成描述。
        - **远程API配置** (当选择“远程API”时):
            - **启用远程服务**: 显示一个必须勾选的复选框 **“允许使用远程服务”** (`enable_remote_services`)，并提示这可能涉及数据传输和费用。
            - **API端点 (URL)**: 一个文本输入框，用于填写API的URL。
            - **请求头 (Headers)**: 一个文本区域，允许用户以JSON格式输入自定义的请求头（如 `{"Authorization": "Bearer YOUR_API_KEY"}`）。
            - **请求参数 (Params)**: 一个文本区域，允许用户以JSON格式输入额外的API请求参数。
            - **提示词 (Prompt)**: 提供一个文本区域，让用户输入自定义的提示词。
- **FR2.5 模型选择 (当选择VLM或ASR流水线时)**:
  - **ASR模型**: 提供下拉菜单让用户选择具体的ASR模型，如 `whisper_tiny`, `whisper_base`, `whisper_turbo`。
  - **VLM模型**:
    - 提供“本地模型”和“远程API”两种模式选择。
    - **本地模型**: 提供下拉菜单选择推理框架 (`Transformers`, `MLX`) 和具体的模型 (`smoldocling`, `granite-vision` 等)。
    - **远程API**: 提供预设服务 (`LM Studio`, `Ollama`, `watsonx.ai`) 的选项和一个“自定义”选项。选择后，显示相应的表单，让用户填写API URL、参数、Headers等信息。
- **FR2.6 图像导出模式**:
  - 提供选项（如单选按钮）让用户选择图像的导出方式：`嵌入式`, `引用式`, `占位符`。
- **FR2.7 输出目录**:
  - 提供一个输入框和文件夹选择按钮，允许用户指定一个目录来保存所有转换结果。
  - 如果留空，则结果默认保存在源文件所在的目录。
- **FR2.8 分块配置 (当模式为“分块输出”时)**:
  - **强制JSON输出**: 当选择此模式时，输出格式应自动设置为 `JSON` 并锁定，因为分块结果是结构化的。
  - **分块策略 (`chunking_strategy`)**: 提供一个下拉菜单让用户选择文档分块策略。默认为 `by_element`（按元素分块）。
  - **排除元素 (`exclude`)**: 提供一个多选框或标签输入框，允许用户指定在分块输出中排除的元素类型（如 `picture`, `table`）。
- **FR2.9 信息提取配置 (当模式为“信息提取”时)**:
  - **强制JSON输出**: 当选择此模式时，输出格式应自动设置为 `JSON` 并锁定。
  - **JSON Schema 输入**: 提供一个文本域，允许用户粘贴或编写用于定义提取规则的 JSON Schema。
  - **Schema 验证**: (可选，但建议) 前端可以对用户输入的JSON Schema进行实时语法验证，提示明显错误。
  - **推荐流水线**: 当启用此功能时，应向用户推荐使用 `VLM` 流水线以获得最佳效果。

- **FR2.10 产物导出 (Artifact Export)**:
  - 提供一组复选框，允许用户在转换的同时导出额外的产物：
    - **导出表格**:
      - [ ] 导出为 CSV
      - [ ] 导出为 HTML
    - **导出图像**:
      - [ ] 导出页面为图像 (PNG)
      - [ ] 导出文档内图表为图像 (PNG)
    - **导出多模态数据**:
      - [ ] 导出为 Parquet 格式

- **FR2.11 高级配置**:
  - **PDF后端**: 提供下拉菜单选择PDF处理后端 (`DoclingParseV4`, `PyPdfium2`)。
  - **硬件加速**: 提供下拉菜单选择设备 (`auto`, `cpu`, `cuda`, `mps`)，并允许为 `cuda` 指定设备ID（如 `cuda:1`）。

- **FR2.12 后处理**:
  - **文本翻译**: 提供一个复选框和目标语言输入框，用于在转换后对文档中的文本进行翻译。

### 2.3 任务执行与监控
- **FR3.1 开始转换**: 一个“开始转换”按钮，点击后触发后端处理。
- **FR3.2 进度显示**:
  - **总体进度**: 显示整个转换任务的总体进度条。
  - **单个文件进度**: 在文件队列中实时更新每个文件的处理状态。
- **FR3.3 实时日志**: 提供一个可展开的日志窗口，实时显示后端的处理日志，帮助用户了解当前状况和排查问题。
- **FR3.4 任务控制**: 提供“取消”按钮，可以中止正在进行中的转换任务。

### 2.4 结果展示与下载
- **FR4.1 结果概览**: 转换完成后，提供一个清晰的结果摘要页面，显示成功和失败的文件数量。
- **FR4.2 单文件结果预览**:
  - 用户可以点击成功转换的文件，在界面上直接预览其内容。
  - **Markdown/Text**: 在代码编辑器或格式化文本区域中显示。
  - **HTML**: 在一个内嵌的iframe或新标签页中渲染显示。
  - **JSON**: 在一个可折叠、格式化的JSON查看器中显示。
- **FR4.3 结果下载**:
  - **单个下载**: 用户可以下载单个转换结果文件。
  - **产物下载**: 如果用户选择了导出额外产物（如表格、图像），这些产物应与主结果文件一起列出并提供单独下载。
  - **打包下载**: 提供一个“全部下载”按钮，将所有结果文件和导出的产物打包成一个ZIP文件供用户下载。
- **FR4.4 错误报告**: 对于转换失败的文件，应明确展示错误信息，并链接到相关的日志条目。

## 3. 非功能需求

### 3.1 用户体验 (UX)
- **NF1.1 易用性**: 界面设计应直观简洁，即使是初次使用的用户也能在没有指导的情况下完成基本操作。
- **NF1.2 响应式设计**: 界面应能适应不同尺寸的屏幕，在桌面浏览器上提供良好体验。
- **NF1.3 交互反馈**: 所有用户操作都应有即时、明确的视觉反馈（如加载动画、状态变化、成功/错误提示）。

### 3.2 性能
- **NF2.1 前端性能**: 界面应保持流畅，即使在处理大量文件队列时也不会出现卡顿。
- **NF2.2 大文件处理**: 前端应能处理大文件的上传，并提供上传进度。

### 3.3 兼容性
- **NF3.1 浏览器兼容**: 支持主流现代浏览器，如 Chrome, Firefox, Safari, Edge的最新版本。

### 3.4 可靠性
- **NF4.1 状态同步**: 前端必须准确地与后端同步任务状态，避免信息不一致。
- **NF4.2 错误处理**: 优雅地处理后端API的错误，并向用户提供清晰的错误提示。

## 4. 技术选型建议

### 4.1 前端框架
- **推荐**: **React** 或 **Vue.js**
  - **理由**: 两者都拥有庞大的社区、丰富的生态系统和成熟的组件库，能够快速构建功能丰富、交互性强的单页应用（SPA）。React的 `Next.js` 或 Vue 的 `Nuxt.js` 框架可以进一步简化开发。

### 4.2 UI 组件库
- **推荐**: **Ant Design** (React/Vue), **Element Plus** (Vue), or **Material-UI (MUI)** (React)
  - **理由**: 提供了一套高质量、开箱即用的UI组件，可以大大加快开发速度，并保证界面风格的统一性和专业性。

### 4.3 状态管理
- **推荐**: **Redux Toolkit** (React) 或 **Pinia** (Vue)
  - **理由**: 用于集中管理应用的全局状态（如文件队列、任务状态、配置选项），使状态逻辑清晰、可预测。

### 4.4 后端通信
- **推荐**: **Axios**
  - **理由**: 一个成熟的、基于Promise的HTTP客户端，支持请求/响应拦截、取消请求等高级功能，非常适合与后端RESTful API进行通信。

### 4.5 打包与构建
- **推荐**: **Vite**
  - **理由**: 提供极快的冷启动速度和热模块更新（HMR），能显著提升开发体验。

### 4.6 桌面应用打包 (可选)
- **推荐**: **Tauri** 或 **Electron**
  - **理由**: 如果需要将Web应用打包成跨平台的桌面应用，Tauri（基于Rust）提供了更轻量、更安全的方案，而Electron（基于Node.js）则拥有更成熟的生态。