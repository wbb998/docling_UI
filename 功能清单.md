# Docling 后端功能清单

本文档旨在全面梳理 Docling 后端系统的核心功能模块，为前端界面的开发提供清晰的功能参考。

## 新增：核心使用场景

### 极简转换 (Minimal Conversion)

这是 `docling` 最核心和最简单的用例，体现了“约定优于配置”的原则。

- **默认行为**: 用户只需提供输入文件（如PDF或图片），无需任何额外配置。
- **默认输入**: 自动处理 `PDF` 和常见的图片格式。
- **默认输出**: 自动转换为 `Markdown` 格式。
- **默认保存**: 结果默认保存在与输入文件相同的目录下，文件名相同，扩展名不同。

这个场景应该作为前端“简单模式”的设计基础。

### 自定义转换 (Custom Conversion)

用户可以灵活地改变输出格式和相关参数。

- **指定输出格式 (`--to`)**: 将文档转换为 `HTML`, `JSON` 等多种格式。
- **自定义图像处理 (`--image-export-mode`)**: 控制输出文档中图像的处理方式，例如将图片保存为单独文件并引用 (`referenced`)。

### 批量转换 (Batch Conversion)

支持对整个目录中的文件进行批量处理。

- **目录作为输入**: 直接将一个文件夹路径作为输入源。
- **指定输出目录 (`--output`)**: 将所有转换结果统一保存到指定的输出文件夹中，而不是默认的源文件目录。

### 按格式转换 (Run with Formats)

允许用户精确控制要处理的文件类型和要生成的输出格式。

- **指定输入格式 (`--from`)**: 仅处理指定类型的输入文件（如 `PDF`），忽略目录中的其他文件。
- **多种输出格式**: 一次性将源文件转换为多种目标格式（如同时生成 `JSON` 和 `HTML`）。

---

## 一、核心转换功能 (`convert`)

这是系统的主要入口点，负责执行所有文档转换任务。

### 1.1 输入源 (Input)

- **多种来源**:
  - [x] **本地文件**: 支持转换单个或多个本地文件。
  - [x] **本地目录**: 支持递归扫描并转换指定目录下的所有符合条件的文件。
  - [x] **URL**: 支持从网络URL下载并转换文件。
- **格式指定 (`--from`)**:
  - [x] 支持指定一种或多种输入文件格式，如 `PDF`, `IMAGE` (JPEG, PNG, etc.), `AUDIO` (WAV, MP3, etc.), `METS_GBS`。
  - [x] 如果不指定，系统会尝试处理所有支持的格式。

### 1.2 输出格式 (Output)

- **格式指定 (`--to`)**:
  - [x] **JSON**: 结构化的文档数据。
  - [x] **YAML**: YAML 格式的结构化文档数据。
  - [x] **HTML**: 可在浏览器中查看的富文本格式。
  - [x] **HTML_SPLIT_PAGE**: 分页视图的HTML格式。
  - [x] **Markdown**: 轻量级标记语言格式。
  - [x] **TEXT**: 纯文本格式。
  - [x] **DOCTAGS**: 自定义的文档标签格式。
- **输出路径 (`--output`)**:
  - [x] 支持指定一个目录用于存放所有转换结果。

### 1.4 特定内容导出 (Artifact Export)
除了完整的文档格式转换，还支持导出文档中的特定部分。
- [x] **表格导出**:
    - 将文档中的每个表格单独导出为 `CSV` 格式。
    - 将文档中的每个表格单独导出为 `HTML` 格式。
- [x] **图像/图表导出**:
    - 将文档中识别出的每个图表 (Picture) 保存为独立的图像文件 (如 `PNG`)。
    - 将文档的每一页本身保存为图像文件。
- [x] **多模态数据导出**:
    - 将每一页的文本、图像、布局信息和元数据打包导出为 `Parquet` 格式，便于进行多模态分析和训练。

### 1.3 图像处理 (`--image-export-mode`)

- **嵌入式 (`embedded`)**: 将图片以Base64编码嵌入到输出文件（如HTML, Markdown）中。
- **引用式 (`referenced`)**: 将图片保存为单独的文件，并在主文档中通过路径引用。
- **占位符 (`placeholder`)** : 仅在输出中标记图片的位置，不包含图片数据。

## 二、处理流水线 (Processing Pipeline)

系统提供不同的处理流水线以适应不同类型的文档和需求。

- **标准流水线 (`--pipeline STANDARD`)**:
  - [x] **PDF 后端选择 (`--pdf-backend`)**:
    - `DoclingParseV4`: 默认的、功能强大的后端。
    - `PyPdfium2`: 一个备选的PDF处理后端。
  - [x] **OCR 支持**:
    - `--ocr`: 启用/禁用OCR。
    - `--force-ocr`: 强制对整个页面进行OCR，忽略任何已有的文本层。
    - `--ocr-engine`: 支持多种OCR引擎，包括：
        - `easyocr`
        - `tesseract`
        - `tesseract_cli`
        - `ocrmac` (仅限macOS)
        - `rapidocr`
    - `--ocr-lang`: 指定OCR识别的语言（如 `en`, `ch_sim`）。
    - `--ocr-lang-auto`: 启用Tesseract的语言自动检测功能。
    - **自定义模型路径 (RapidOCR)**: 允许为RapidOCR提供自定义的ONNX模型文件路径。
  - [x] **表格结构识别**: 自动检测文档中的表格并提取其结构。
  - [x] **内容增强**:
    - `--enrich-code`: 识别和增强代码块。
    - `--enrich-formula`: 识别和增强数学公式。
    - `--enrich-picture-classes`: 对图片进行分类。
  - [x] **图片描述生成 (Picture Annotation/Description)**:
    - **启用功能 (`do_picture_description`)**: 一个总开关，用于启用或禁用此功能。
    - **本地模型描述**: 支持使用在本地运行的VLM（视觉语言模型）为图片生成描述。
        - **内置模型支持**: 简化了对 `ibm-granite/granite-vision` 和 `HuggingFaceTB/SmolVLM` 等常用模型的调用。
        - **自定义本地模型**: 允许用户通过 `PictureDescriptionVlmOptions` 指定任意 Hugging Face Hub 上的模型 `repo_id`。
        - **自定义提示词**: 用户可以提供自定义的 `prompt` 来精确控制描述的风格和内容。
    - **远程模型描述**: 支持通过调用外部API来生成图片描述。
        - **启用远程服务 (`enable_remote_services`)**: 使用此功能前必须启用该选项。
        - **灵活的API配置**: 通过 `PictureDescriptionApiOptions` 支持与任何兼容OpenAI聊天格式的API端点集成。
        - **集成示例**: 提供与 `vLLM`, `LM Studio`, `IBM watsonx.ai` 等服务的配置示例。
        - **完全可配置**: 允许用户自定义API的 `url`, `headers` (用于认证), `params` (请求参数), 和 `prompt`。
- **视觉语言模型流水线 (`--pipeline VLM`)**:
  - [x] 使用先进的视觉语言模型处理文档。
  - [x] **本地模型**: 支持多种在本地运行的模型和推理框架。
    - **模型示例**: `smoldocling`, `granite-vision`, `phi-3-vision`, `llava-qwen` 等。
    - **推理框架**: `Transformers`, `MLX` (适用于Apple Silicon)。
  - [x] **远程模型 (API)**: 支持通过API调用外部VLM服务。
    - **集成示例**: `LM Studio`, `Ollama`, `IBM watsonx.ai`。
    - **可配置性**: 允许用户自定义API的URL、请求参数、Headers和Prompt模板。
- **语音识别流水线 (`--pipeline ASR`)**:
  - [x] 用于处理音频和视频文件。
  - [x] **模型选择 (`--asr-model`)**: 支持 `whisper_tiny`, `whisper_base`, `whisper_large`, `whisper_turbo` 等多种Whisper模型。

## 三、高级配置与控制

### 3.1 性能 (`--num-threads`, `--page-batch-size`, `--device`)

- [x] **多线程**: 设置用于处理的线程数。
- [x] **批处理大小**: 设置一次处理的页面数量。
- [x] **硬件加速**: 选择使用的计算设备（`auto`, `cpu`, `cuda`, `cuda:N`, `mps`）。

### 3.2 调试与可视化

- [x] **布局可视化 (`--show-layout`)**: 在输出的页面图像上显示元素的边界框。
- [x] **详细日志 (`-v`, `-vv`)**: 控制日志输出的详细程度。
- [x] **高级调试 (`--debug-*`)**:
    - `--debug-visualize-cells`: 可视化PDF单元格。
    - `--debug-visualize-ocr`: 可视化OCR结果。
    - `--debug-visualize-layout`: 可视化布局聚类。
    - `--debug-visualize-tables`: 可视化表格单元格。

### 3.3 错误处理

- [x] **错误时中止 (`--abort-on-error`)**: 在遇到第一个转换错误时立即停止处理。
- [x] **文档超时 (`--document-timeout`)**: 为处理单个文档设置超时时间。

## 四、系统与插件

- [x] **版本信息 (`--version`)**: 显示系统各组件的版本号。
- [x] **外部插件**:
  - `--allow-external-plugins`: 允许加载第三方插件（如OCR引擎）。
  - `--show-external-plugins`: 显示当前可用的第三方插件列表。

## 五、序列化与分块 (Serialization & Chunking)

为了方便下游应用（如RAG），系统支持对转换后的文档进行分块和自定义序列化。

- **文档分块 (`chunking_strategy`)**:
  - [x] **按元素分块 (`by_element`)**: 将每个独立的文档元素（如段落、表格）作为一个块。这是默认的分块策略。
- **序列化选项**:
  - [x] **排除元素 (`exclude`)**: 在输出时可以排除特定类型的元素（如 `picture`, `table`）。
  - [x] **包含元数据**: 在分块输出时，每个块默认包含其来源页码等元数据。
  - [x] **JSON输出格式**: 分块和序列化功能通常与 `JSON` 输出格式结合使用，以生成结构化的数据块。

## 六、信息提取 (Information Extraction)

除了完整的文档转换，系统还支持从文档中提取特定的、结构化的信息。

- **基于模式的提取 (`--extraction-schema`)**:
  - [x] **提供JSON Schema**: 用户可以提供一个JSON Schema来定义需要提取的数据字段、类型和描述。
  - [x] **结构化输出**: 系统会根据Schema，从文档中提取信息并以匹配该Schema的JSON格式输出。
- **与VLM流水线结合**:
  - [x] 该功能通常与VLM（视觉语言模型）流水线结合使用，利用大模型的能力来理解和提取信息。

## 七、自定义后端与集成

- [x] **自定义XML后端**:
  - 系统支持为特定的XML格式开发自定义后端。
  - **内置示例**: `JATS` (用于PubMed文章), `USPTO` (用于美国专利)。
- [x] **RAG集成 (以LlamaIndex为例)**:
  - 转换和分块后的文档可以轻松集成到RAG（检索增强生成）工作流中。
  - 提供与`llama-index-readers-docling`和`llama-index-node-parser-docling`等扩展的兼容性。

## 八、文档后处理

- [x] **在内存中修改文档**:
  - `DoclingDocument` 对象支持在转换后、导出前进行遍历和修改。
  - **用例**: 可以在内存中对所有文本元素执行翻译、内容替换等操作。