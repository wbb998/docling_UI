# 项目文件系统结构设计

本设计旨在为 Docling UI 项目提供一个可扩展、易维护、便于与开源 docling 后端解耦升级的目录结构。目标：
- 清晰划分核心业务模块与支撑模块
- 独立第三方依赖与对接适配层，降低升级成本
- 完整沉淀文档资源与版本控制策略
- 保持与 docling 后端的标准化对接接口，未来可平滑升级

适用环境：Windows 10/11（PowerShell 7），UTF-8 编码，中文友好。

---

## 1. 顶层目录结构（建议）

```text
docling_UI/
├─ apps/                              # 前端/客户端应用层（面向用户）
│  ├─ web/                            # Web 前端 (React/Vue 任一)
│  │  ├─ src/                         # 前端源代码
│  │  │  ├─ modules/                  # 业务模块（与功能清单一致：转换/分块/提取/图片描述/产物导出 等）
│  │  │  ├─ components/               # 通用 UI 组件（表单、进度、预览、日志等）
│  │  │  ├─ pages/                    # 路由页面（简单模式/高级模式/结果页/任务详情）
│  │  │  ├─ services/                 # 前端 API 客户端（与 /services/api-gateway 的 OpenAPI 同步）
│  │  │  ├─ store/                    # 全局状态（配置项、队列、任务状态）
│  │  │  ├─ hooks/                    # 自定义 Hook（表单联动/动态校验）
│  │  │  ├─ utils/                    # 工具方法（参数映射、下载、格式化）
│  │  │  └─ types/                    # TS 类型（与 API 契约/Schema 对齐）
│  │  ├─ public/
│  │  ├─ scripts/                     # 开发与调试脚本（Windows）
│  │  │  ├─ kill-port.ps1             # 启动前强制释放端口（满足“先 kill-port 再起服”）
│  │  │  ├─ start-dev.ps1             # 调用 kill-port 后再启动 dev 服务器
│  │  │  └─ build-release.ps1         # 打包构建（含产物清理）
│  │  ├─ .editorconfig                # 统一编码/行尾（utf-8 + crlf）
│  │  ├─ .gitattributes               # 文本规范（text=auto eol=crlf）
│  │  ├─ package.json                 # 前端依赖与脚本（锁版本）
│  │  └─ pnpm-lock.yaml               # 依赖锁定（建议 pnpm）
│  └─ desktop/                        # 可选：桌面应用 (Tauri/Electron) 的 UI 容器
│     └─ ...
│
├─ services/                          # 轻服务/网关层（与 docling 解耦）
│  ├─ api-gateway/                    # 单一端点 /convert，组合式参数实现全部能力
│  │  ├─ src/
│  │  │  ├─ adapters/                 # 适配层：对接 docling（关键）
│  │  │  │  └─ docling/
│  │  │  │     ├─ cli-adapter.ts      # 通过 CLI 调用 docling（spawn/管道/退出码转换）
│  │  │  │     ├─ sdk-adapter.ts      # 通过 Python SDK/HTTP 桥接（可选方案）
│  │  │  │     ├─ mappers/            # 参数映射（UI→API→docling CLI/SDK）
│  │  │  │     │  ├─ convert.mapper.ts
│  │  │  │     │  ├─ chunking.mapper.ts
│  │  │  │     │  └─ extraction.mapper.ts
│  │  │  │     ├─ VERSION             # 已验证的 docling 版本号（如 0.14.x）
│  │  │  │     └─ feature-matrix.json # 版本-特性矩阵（用于动态显隐与兼容检测）
│  │  │  ├─ controllers/
│  │  │  │  └─ convert.controller.ts  # 校验请求→调用 service→返回统一响应
│  │  │  ├─ services/
│  │  │  │  ├─ convert.service.ts     # 组合参数→调用 adapter→汇总结果/错误
│  │  │  │  └─ jobs.service.ts        # 异步任务/job_id 状态管理（内存/Redis/SQLite 均可）
│  │  │  ├─ middlewares/              # 日志/TraceId/幂等键/错误中文化
│  │  │  ├─ schemas/                  # 请求/响应 JSON Schema（与前端 types 对齐）
│  │  │  │  ├─ convert.request.schema.json
│  │  │  │  └─ error.schema.json
│  │  │  ├─ routes/
│  │  │  │  └─ index.ts               # /convert, /jobs/:id/status, /jobs/:id/result, /jobs/:id/events
│  │  │  ├─ utils/                     # 进程/超时/临时目录/安全（URL 校验、脱敏）
│  │  │  └─ index.ts                   # 应用入口（读取 configs/，注册路由/中间件）
│  │  ├─ openapi/
│  │  │  └─ convert.yml                # 与《后端API接口文档.md》保持一致的契约源文件
│  │  ├─ configs/                      # 网关运行配置（按环境分层）
│  │  │  ├─ default.json
│  │  │  ├─ dev.json
│  │  │  └─ prod.json
│  │  ├─ .env.example                  # 环境变量模板（不含敏感信息）
│  │  ├─ package.json / pyproject.toml # 网关实现语言自选（Node 或 Python）
│  │  └─ lockfile                      # 锁版本（npm/pnpm/poetry/pip-tools）
│  └─ workers/                         # 队列/长任务处理（大文件/批处理）
│     └─ ...
│
├─ integrations/                      # 第三方对接与版本管理（升级友好）
│  ├─ docling/
│  │  ├─ manifest.json                # 固定 docling 版本 & 能力描述（与 adapters/feature-matrix 对齐）
│  │  ├─ constraints.txt              # Python 依赖约束（pip-tools/constraints）
│  │  ├─ upgrade-playbook.md          # 升级手册（检查点、测试集、回滚点）
│  │  └─ changelog-tracker.md         # 跟踪上游变更点→映射到本项目影响
│  ├─ vlm_providers/                  # LM Studio/Ollama/watsonx 等远程 VLM 预设配置
│  │  ├─ lm-studio.json
│  │  ├─ ollama.json
│  │  └─ watsonx.json
│  └─ asr_providers/                  # 可选：ASR/音视频引擎预设
│     └─ ...
│
├─ configs/                           # 全局配置中心（前后端共享原则/Schema）
│  ├─ schema/                         # JSON Schema（驱动前端表单 & 网关校验）
│  │  ├─ app.schema.json
│  │  └─ security.schema.json
│  ├─ default/
│  │  ├─ app.json
│  │  └─ security.json
│  ├─ dev/
│  ├─ prod/
│  └─ secrets/                        # 机密（被 .gitignore，使用 .env.* 方式注入）
│
├─ storage/                           # 可写目录（上传/输出/产物）
│  ├─ uploads/
│  ├─ outputs/
│  └─ artifacts/
│
├─ docs/                              # 文档与资源（版本化/面向团队）
│  ├─ functional/                     # 功能文档（功能清单.md 等）
│  ├─ requirements/                   # 需求文档（项目需求文档.md）
│  ├─ flows/                          # 业务流程（业务流程图.md）
│  ├─ api/                            # API 文档（后端API接口文档.md & OpenAPI 导出）
│  ├─ examples/                       # 示例用例与期望结果（覆盖 Minimal/Custom/Batch/Formats/Serialization/Extraction/Picture）
│  ├─ maintenance/
│  │  ├─ docling-upgrade.md           # 与 integrations/docling 对应的操作指南
│  │  └─ versioning-policy.md         # 版本规范（SemVer/锁版本策略/回滚流程）
│  └─ README.md
│
├─ test/                              # 测试（保证升级不破坏能力）
│  ├─ unit/
│  ├─ integration/
│  │  ├─ adapters.docling.spec.ts     # 适配层关键映射的集成测试
│  │  └─ api.convert.spec.ts          # /convert 全参组合/冲突用例
│  └─ e2e/
│     └─ scenarios/                   # 覆盖四大示例维度与边界条件
│
├─ scripts/                           # 通用脚本（CI/运维/Windows 适配）
│  ├─ win/
│  │  ├─ kill-port.ps1                # 强制释放端口（严格在 dev 前执行）
│  │  ├─ verify-docling-compat.ps1    # 拉取 manifest，跑回归样例验证
│  │  └─ export-artifacts.ps1         # 统一导出产物（CSV/HTML/PNG/Parquet）
│  └─ ci/
│     ├─ run-unit.ps1
│     ├─ run-integration.ps1
│     └─ run-e2e.ps1
│
├─ .vscode/                           # 开发环境建议（确保中文/UTF-8/CRLF）
│  ├─ settings.json
│  └─ tasks.json
│
├─ .editorconfig
├─ .gitattributes
├─ .gitignore
├─ README.md
└─ LICENSE
```

---

## 2. 模块边界与职责

- apps/web：用户交互层。以“模块化+可组合”为核心设计（简单/高级模式切换；联动校验；参数冲突提示；远程能力前置依赖开关）。
- services/api-gateway：统一 `/convert` 端点，聚合“转换/分块/提取/图片描述/产物导出”等能力，返回统一结构、中文错误提示、支持异步 job_id 与进度/结果接口。
- adapters/docling：与 docling 的解耦层。对上提供稳定接口，对下可自由切换 CLI/SDK/HTTP 桥接；通过 feature-matrix 与 VERSION 控制兼容范围。
- integrations/docling：版本管理与升级手册，固定 docling 版本与能力，配合脚本做可回归验证。
- docs：沉淀功能/需求/流程/API 文档与示例，用于前后端对齐与回归。
- test：覆盖四大示例（Conversion、Serialization & Chunking、Information Extraction、Picture Annotation）与边界条件的回归集。

---

## 3. 标准化对接接口（契约）

- 契约来源：`services/api-gateway/openapi/convert.yml` 与 `docs/api/后端API接口文档.md` 同步维护。
- 核心端点：
  - POST `/convert`：单端点覆盖全部组合参数。
  - GET `/jobs/{id}/status`、`/jobs/{id}/result`、SSE `/jobs/{id}/events`（可选增强）。
- 约束规则（服务端强校验）：
  - 选择 `extraction_schema` 或 `chunking_strategy` → 输出强制 JSON；与 `to_formats` 同时出现 → 400（E001_INVALID_INPUT）。
  - 远程图片描述/远程 VLM 仅当 `enable_remote_services=true`。
  - `image_export_mode` 对纯 JSON 输出不产生可见效果，除非导出 `pages_png/pictures_png`。
- 错误响应（中文）：
  ```json
  { "code": "E001_INVALID_INPUT", "message": "参数组合冲突：已选择信息提取，输出格式固定为 JSON，请移除 to_formats。", "details": { } }
  ```

---

## 4. 第三方依赖与版本管理

- 前端：pnpm 锁版本；不提交敏感配置（.env.* 通过模板下发）。
- 网关（Node/Python 任选其一）：
  - Node：package.json + pnpm-lock.yaml（严格锁定）。
  - Python：pip-tools/poetry + `integrations/docling/constraints.txt` 固定上游依赖。
- docling 版本：
  - `adapters/docling/VERSION`：记录已通过回归验证的上游版本（如 0.14.x）。
  - `integrations/docling/manifest.json`：记录版本与能力矩阵（用于前端动态显隐）。
  - `integrations/docling/upgrade-playbook.md`：升级步骤、测试清单、回滚策略。
- CI 验证：
  - `scripts/win/verify-docling-compat.ps1`：对 docs/examples 的样例跑通 → 产物对比 → 判定兼容性。

---

## 5. 文档资源与版本控制

- 将四份核心文档（功能清单.md、项目需求文档.md、业务流程图.md、后端API接口文档.md）放置于 `docs/functional|requirements|flows|api` 子目录，并在 `docs/examples` 存放示例输入/期望输出。
- `docs/maintenance/versioning-policy.md`：统一 SemVer 规则与锁版本策略。
- `.editorconfig`、`.gitattributes`：统一 UTF-8 与 CRLF，保证中文显示与跨平台行尾一致性。

---

## 6. Windows 中文环境与开发脚本

- `apps/web/scripts/kill-port.ps1`：启动 dev 前强制释放端口（配合 `start-dev.ps1`）。
- `.vscode/settings.json`：统一编码为 UTF-8，终端使用 PowerShell 7，确保中文日志正常显示。
- 错误提示规范：所有 CLI 与 API 返回中文可读提示（前端映射错误码→用户友好提示）。

---

## 7. 升级兼容策略（面向 docling）

- 适配层隔离：对 docling 仅在 `adapters/docling/**` 中出现；升级时只需更改映射与兼容逻辑。
- 能力矩阵驱动：前端 UI 根据 `feature-matrix.json` 动态显隐功能与参数，避免不兼容组合。
- 回归集保障：对 Minimal/Custom/Batch/Formats、Serialization & Chunking、Information Extraction、Picture Annotation 四类样例做基线对比。
- 渐进式开关：新特性以“实验性（实验开关）”进入，默认关闭；稳定后纳入常规能力。

---

## 8. 与当前文档的一致性映射

- 功能清单 → apps/web/src/modules 与 services/api-gateway/src/schemas 的字段一一对应。
- 项目需求文档（动态联动/冲突校验/远程依赖）→ 前端表单逻辑 + 网关中间件强校验。
- 业务流程图（参数校验/异步 job_id）→ routes + services/jobs.service 实现。
- 后端 API 文档（单端点 + 参数约束 + 错误码）→ openapi/convert.yml 为单一事实来源（Single Source of Truth）。

---

## 9. 后续可选增强

- 引入配置预设仓库（apps/web/src/presets），支持导入/导出 JSON。
- 提供本地“样例工场”（docs/examples + UI 入口）用于快速回归演示。
- 引入插件系统（apps/web 插件/ services/api-gateway 插件），通过 `allow_external_plugins` 与 `show_external_plugins` 控制。

以上结构兼顾“模块化+可组合”的业务目标与“解耦+可升级”的工程诉求，能在 docling 后端版本演进下保持最小改动面与快速回归验证能力。