# Docling 核心业务流程图

本文档使用 Mermaid 流程图详细描述了 Docling 系统的核心功能——文档转换 (`convert`) 的业务流程。

## 极简转换业务流程 (Minimal Conversion Flow)

此流程对应默认的、最简单的使用场景。

```mermaid
graph TD
    A[用户上传文件<br/>(如 document.pdf)] --> B[点击“转换”按钮];
    B --> C{系统接收请求<br/>(无额外参数)};
    C --> D[应用默认配置<br/>- 输出格式: Markdown<br/>- 流水线: Standard<br/>- ...其他默认值];
    D --> E[执行标准转换流水线];
    E --> F[生成 Markdown 内容];
    F --> G[保存为 .md 文件<br/>(如 document.md)];
    G --> H[向用户提供下载链接];

    style A fill:#f9f,stroke:#333,stroke-width:2px
    style H fill:#f9f,stroke:#333,stroke-width:2px
```

---

## 完整转换流程 (高级模式)

```mermaid
graph TD
    A[用户启动转换任务] --> B{选择输入源};
    B --> B1[文件/文件夹];
    B --> B2[URL];

    A --> C{配置转换选项};
    C --> C0[选择输出模式<br/>(转换, 分块, 提取)];
    C --> C1[选择处理流水线<br/>(Standard, VLM, ASR)];
    C --> C2[选择输出格式<br/>(在“转换”模式下)];
    C --> C3[配置图像、OCR、增强等高级选项];
    C --> C4[配置分块/提取选项<br/>(在对应模式下)];
    C --> C5[选择要导出的额外产物<br/>(表格, 图像, 多模态数据)];
    C --> C6[配置后处理步骤<br/>(如翻译)];

    subgraph 系统处理
        D[系统初始化<br/>- 解析参数<br/>- 准备输入文件]
        E{根据文件类型和流水线选择处理器}
        F[执行转换流水线]
        G{处理结果}
    end

    B1 & B2 & C0 & C1 & C2 & C3 & C4 --> D;
    D --> E;

    E -- PDF/Image --> E_PDF[PDF/图像处理器];
    E -- Audio/Video --> E_ASR[音频/视频处理器];
    E -- Other --> E_OTHER[其他格式处理器];

    subgraph "Standard 流水线"
        direction LR
        E_PDF --> S1[PDF后端解析<br/>(e.g., DLParse)];
        S1 --> S2{是否启用OCR?};
        S2 -- 是 --> S3[执行OCR];
        S2 -- 否 --> S4;
        S3 --> S4[布局分析与内容提取];
        S4 --> S5[表格/公式/代码等内容增强];
        S5 --> S6{是否启用图片描述?};
        S6 -- 是 --> S7{模型类型?};
        S7 -- 本地 --> S8_LOCAL[使用本地VLM生成描述];
        S7 -- 远程 --> S8_REMOTE[调用远程API生成描述];
        S8_LOCAL --> S9[合并描述到文档];
        S8_REMOTE --> S9;
        S6 -- 否 --> S9;
    end

    subgraph "VLM 流水线"
        direction LR
        E_PDF --> V0{选择VLM模型类型}
        V0 -- 本地 --> V1_LOCAL[加载本地模型<br/>(Transformers/MLX)];
        V0 -- 远程 --> V1_REMOTE[配置远程API<br/>(Ollama/LM Studio)];
        V1_LOCAL --> V2[模型处理页面图像];
        V1_REMOTE --> V2;
        V2 --> V3[生成结构化描述];
    end

    subgraph "ASR 流水线"
        direction LR
        E_ASR --> A1[加载ASR模型<br/>(e.g., Whisper)];
        A1 --> A2[执行语音到文本转换];
    end

    S9 --> F;
    V3 --> F;
    A2 --> F;
    E_OTHER --> F;

    F --> G;
    G -- 成功 --> H[生成结构化文档对象];
    G -- 失败 --> I[记录错误信息];

    H --> PROC_MODE{根据输出模式处理};
    PROC_MODE -- **文档转换** --> J{导出为指定格式};
    PROC_MODE -- **分块输出** --> CHUNK[执行分块策略] --> EXPORT_CHUNK_JSON[导出为分块JSON];
    PROC_MODE -- **信息提取** --> EXTRACT[执行信息提取] --> EXPORT_EXTRACT_JSON[导出为提取后的JSON];

    J --> J_PATH{检查是否指定输出目录};
    EXPORT_CHUNK_JSON --> J_PATH;
    EXPORT_EXTRACT_JSON --> J_PATH;

    J_PATH -- 是 --> J_SAVE[保存到指定目录];
    J_PATH -- 否 --> J_SAVE[保存到源文件目录];

    J_SAVE -- Markdown --> J1[保存为 .md 文件];
    J_SAVE -- HTML --> J2[保存为 .html 文件];
    J_SAVE -- JSON --> J3[保存为 .json 文件];
    J_SAVE -- ... --> J4[...];

    H --> POST_PROC{是否需要后处理?};
    POST_PROC -- 是 --> PP1[执行翻译等操作] --> PROC_MODE;
    POST_PROC -- 否 --> PROC_MODE;

    H --> EXPORT_ARTIFACTS{是否导出额外产物?};
    EXPORT_ARTIFACTS -- 是 --> EA1[导出表格(CSV/HTML)];
    EA1 --> EA2[导出图像(PNG)];
    EA2 --> EA3[导出多模态数据(Parquet)];
    EA3 --> L;


    I --> K[向用户报告错误];
    J1 & J2 & J3 & J4 --> L[任务完成];
    K --> L;

    style A fill:#f9f,stroke:#333,stroke-width:2px
    style L fill:#f9f,stroke:#333,stroke-width:2px
```

### 流程说明

1.  **启动与配置 (A, B, C)**: 用户通过前端界面发起一个转换任务，上传或指定文件/URL，并选择所需的处理流水线、输出格式以及其他高级选项。
2.  **系统初始化 (D)**: 前端将这些信息整合成一个API请求发送给后端。后端接收到请求后，开始初始化，包括解析所有参数、下载网络文件、展开文件夹等，准备好待处理的文件列表。
3.  **选择处理器 (E)**: 系统遍历文件列表，根据每个文件的格式（如PDF、PNG、WAV）和用户选择的流水线，为其分配一个合适的处理器。
4.  **执行流水线 (S, V, A)**:
    *   **Standard 流水线**: 针对PDF和图片，依次执行PDF解析、OCR（如果启用）、布局分析和内容增强（如表格识别）。
    *   **VLM 流水线**: 使用视觉语言模型直接从文档页面图像中提取内容和结构。
    *   **ASR 流水线**: 使用语音识别模型将音频或视频文件中的语音转换为文字。
5.  **生成结果 (F, G, H, I)**: 每条流水线执行完毕后，会生成一个包含所有结构化信息的文档对象（成功时）或一个详细的错误报告（失败时）。
6.  **导出与完成 (J, K, L)**:
    *   对于成功转换的文档，系统会根据用户选择的输出格式（Markdown, HTML等）将其序列化并保存到指定的输出目录。
    *   对于失败的文档，错误信息会被记录下来并报告给用户。
    *   所有文件处理完毕后，任务结束。