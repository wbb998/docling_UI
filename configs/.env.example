# 环境变量示例（不上传真实密钥）
# 将本文件复制为 .env 并按需填写，.env 应加入 .gitignore

# 远程 VLM / 外部服务密钥（仅服务端读取）
REMOTE_VLM_API_KEY=请在本机环境中设置  # 示例：不会提交到仓库

# 日志级别（可选）
LOG_LEVEL=info  # 可选：error/warn/info/debug

# 任务超时（秒）
DOCUMENT_TIMEOUT=600  # 示例默认 10 分钟